# FastAPI for building backend API
fastapi

# Uvicorn for ASGI server for FastAPI
uvicorn

# Neo4j Python driver to interact with knowledge graph database
neo4j-driver

# Celery to manage async tasks and background jobs
celery

# FAISS for fast vector similarity search
faiss-cpu

# Hugging Face Transformers for integrating generative models
transformers

# LangChain to build retrievel-augmented generation pipeline
langchain

# PrettyMIDI for generating and processing MIDI files
pretty_midi # Music stuff

# Mido for MIDI file handling
mido

# Python Dotenv to loaad environment variables from .env
python-dotenv

# Pydantic (dependency of FastAPI) for data validation
pydantic
